# Installation

WindBar does not yet have a finalized public installer.

The v1 release plan includes a signed installer with supported install, upgrade, reinstall, and uninstall paths, plus optional startup behavior.

Do not treat development publish folders or local test builds as the final installation model.

This document will be expanded before public beta with exact installation requirements and upgrade notes.
