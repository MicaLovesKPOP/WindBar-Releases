# Recovery

A public WindBar release must preserve a safe path back to the native Windows shell/taskbar.

The release plan includes recovery behavior for normal exit, crashes, forced termination, Explorer restarts, corrupted settings, and plugin failures.

Exact end-user recovery steps will be documented here before public beta. Until then, this file should not be treated as final support guidance.
